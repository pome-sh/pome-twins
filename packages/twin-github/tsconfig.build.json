{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "."
  },
  "//": "scripts/ is dev/CI tooling run via tsx on the .ts source (capture:fixtures, fidelity:parity, …), never imported by src and not shipped in dist. Typecheck (tsconfig.json, --noEmit) still covers scripts/.",
  "include": ["src/**/*.ts", "examples/**/*.ts"],
  "exclude": ["dist", "node_modules", "test", "scripts"]
}
